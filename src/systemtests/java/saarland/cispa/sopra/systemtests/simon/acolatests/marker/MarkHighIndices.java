package saarland.cispa.sopra.systemtests.simon.acolatests.marker;

import saarland.cispa.sopra.systemtests.simon.BaseTest;
import saarland.cispa.sopra.systemtests.GameInfo;

public class MarkHighIndices extends BaseTest {

    @Override
    protected void test(GameInfo gameInfo) {
    /*    setGameInfo(gameInfo);

        String map = getMapper().toString();
        String brain = createNewBrain( "mark 0", "mark 6", "mark 2", "jump 0");

        simulate(3, 42, map, brain, brain);

        // initial direction: northwest
        verifyMarker(0, 0, 'A', true, false, true, false, false, false, true);
        verifyMarker(1, 0, 'B', false, false, false, false, false, false, false);
    */}
}
